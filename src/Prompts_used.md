User: fix the blank Article Preview page
User: Continue
User: the product carousel in articles on http://localhost:3000/ are not showing thr correct product set that was selected for the article
User: when a product carousel item is clicked it should open 'https://app.kamiunlimited.com/collection/'+collectionid new tab
User: in the featured products board, when a product is added to a set, save the collection id into the database as well.
User: its not saving the collection id from the api, its saving id, i need the collection id
User: i cant scroll or drag the product carousel with the mouse
User: `button` make the share button height same as `button`  on the http://localhost:3000/feed page
User: the button height are still not the same
User: make the `button` button same style as `button` on the http://localhost:3000/feed
User: `div` recommended article carousel on the http://localhost:3000/feed page should show list of published articles `div` shows the cover composite, `span` show the category, `h3` show the title.
User: when the user clicks on an article in the recommended article carousel, it should scroll to the article in the http://localhost:3000/feed page
User: in the recomendded articles carousel, follow the cover composite aspect ration so the image is not cut off `div`
User: `div` increase the size of the cover composite image in recomended articles carousel 30% larger
User: `div` add the Text "Recommended For You" above the Recommended Carousel
User: `button` remove the Browse All Button
